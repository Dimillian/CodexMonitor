# Local iOS device build overrides for scripts/build_run_ios_device.sh.
# Copy this file to .ios.local.env and set your local values.

# Required for code signing if not set in src-tauri/tauri.ios.conf.json.
APPLE_DEVELOPMENT_TEAM=

# Optional defaults used by scripts/build_run_ios_device.sh.
# Device identifier for --device (UDID from `./scripts/build_run_ios_device.sh --list-devices`).
# IOS_DEVICE=
# IOS_BUNDLE_ID=com.dimillian.codexmonitor.ios
# IOS_TARGET=aarch64
